<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Skein Tracker</title>
    </head>
    <body>
        <h1><%= title %></h1>
        <ul>
            <% for (var i=0; i < items.length; i++){ %>
            <li><% items[i].desc %></li> 
            <% } %>
        </ul>

        <form action="/add" method="POST">
            <input type="text" name="newItem">
            <input type="submit">
        </form>
    </body>


</html>